## 1. Tests (write first)

- [x] 1.1 Write test: `PackResource` stores id, name, label and properties are readonly
- [x] 1.2 Write test: `CardResource` stores all required fields and properties are readonly
- [x] 1.3 Write test: `CardResource` nullable fields accept null

## 2. Implementation

- [x] 2.1 Create `src/Resources/PackResource.php` as a readonly DTO with id, name, label
- [x] 2.2 Create `src/Resources/CardResource.php` as a readonly DTO with all card fields
